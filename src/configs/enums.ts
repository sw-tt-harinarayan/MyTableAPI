export enum Category {
  CRIME = "Crime",
  ADVENTURE = "Adventure",
}

export enum Role {
  ADMIN = "admin",
  MANAGER = "manager",
  CUSTOMER = "customer",
}

export enum Status {
  ACTIVE = "Active",
  BLOCKED = "Blocked",
  INACTIVE = "Inactive",
}
